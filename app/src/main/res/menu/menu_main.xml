<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
      xmlns:tools="http://schemas.android.com/tools"
      tools:context="com.mgodevelopment.currencyapp.MainActivity">

    <item
        android:id="@+id/action_show_fab"
        android:icon="@drawable/ic_remove"
        android:title="@string/menu_fab_show"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/action_show_logs"
        android:icon="@drawable/ic_keyboard_hide"
        android:title="@string/menu_show_logs"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/action_clear_logs"
        android:icon="@drawable/ic_clear"
        android:title="@string/menu_clear_logs"
        app:showAsAction="ifRoom"/>

</menu>
